/**
 * We can return a keyword
 * We can return tags
 * conditional statements AND, NOT, OR
 * We can group statements () 
 * 
 * (sze < 5000 ) NOT (ext="xmi") OR (CMC == 2 AND AVGTTR <= 2) 
 * conformsTo="unique_name" hasAttribute="name" AND isTransformable="ID"
 */